{% set name = "koopa-viz" %}
{% set version = "0.0.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 10b7e1df2ec67e999aa5bf24901eb6346e6ffcf5b37079ae9a7a7530140505f5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools
  run:
    - napari
    - numpy
    - pandas
    - pyarrow
    - python >=3.8
    - qtpy
    - scikit-image
    - tifffile
    - trackpy

test:
  imports:
    - koopa_viz
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/bbquercus/koopa-viz
  summary: Vizualization plugin for koopa image analysis
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
